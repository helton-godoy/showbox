# SB-018 — Release candidate v1.0.0-rc.6 (com SB-017)

Estado: em andamento.

- Objetivo: produzir, validar e publicar `v1.0.0-rc.6` a partir da `main`
  atual, contendo a SB-017 integrada, como prerelease curto antes de decidir
  `v1.0.0`.
- Base: `5307ad5` (`main` com a SB-017 integrada via PR #40).
- Branch: `chore/SB-018-rc6`.
- Responsável: integrador desta sessão.
- Dependências: SB-012 (roteiro de RC), SB-013 (RC.5 publicado), SB-017
  (automação do Studio, integrada).

## Escopo

- Atualizar roadmap (linhas SB-017 e SB-018), status e documentos da SB-017,
  que ainda dizem “aguardando revisão/integração”.
- Atualizar a fonte única para `1.0.0-rc.6`, integrar por PR e criar a tag
  protegida sobre o merge aprovado.
- Confirmar tag, commit-alvo, workflow, notas e sete assets do novo draft.
- Baixar os assets do RC.6, registrar checksums SHA-256 e repetir os smokes
  de instalação DEB Ubuntu/Debian, RPM e AppImage.
- Executar o roteiro do fluxo visual da SB-010 contra o produto empacotado.
- Validar os itens SB-017 no produto empacotado: socket JSON-RPC,
  `showbox-studioctl`, adaptador MCP, comportamento sem `--automation` e
  autorização explícita de execução.
- Publicar o draft RC.6 como prerelease sem alterar seus artefatos ou tag.
- Registrar evidências, limitações e abrir a janela curta de uso real antes
  de decidir `v1.0.0`.

## Fora do escopo

- Novas features ou mudanças de comportamento além da fonte única de versão
  e da documentação de acompanhamento.
- Promover diretamente `v1.0.0` ou publicar em repositórios de terceiros.
- Alterar os assets ou as tags já publicados (`rc.4`, `rc.5`).
- Adicionar assinatura, notarização ou novos formatos de pacote.

## Aceite

- `VERSION=1.0.0-rc.6` com conversões validadas (app/deb/rpm/prerelease).
- PR integrado com `build-test`, `sanitizers`, `trunk-check` e SonarCloud
  verdes; `just doctor`, `just build`, `just test` e `just check` aprovados.
- Tag protegida `v1.0.0-rc.6` sobre o merge; workflow com os sete assets e
  `install-smoke` aprovado; release publicada com `prerelease: true`.
- Smokes locais + fluxo visual + itens SB-017 aprovados sobre os assets
  baixados, ou publicação bloqueada com a falha documentada.
- Handoff com SHAs, checksums, testes e limitações.

## Bloqueio encontrado e corrigido

- O workflow da tag `v1.0.0-rc.6` (run `34735435629`) falhou em
  `deb-ubuntu`, `deb-debian` e `rpm`: a SB-017 instalava `showbox-studioctl`
  e `showbox-studio-mcp` em `bindir`, mas as listas de empacotamento não os
  incluíam (`dh_missing --fail-missing` no DEB;
  `_unpackaged_files_terminate_build` no RPM). AppImage passou (componente
  `showbox-studio` já os contém).
- Correção: ambos os binários nas listas `-studio` (DEB/RPM) + smoke
  estendido (presença, `--help`, posse pelo pacote Studio e ausência no
  pacote do motor). Tag `v1.0.0-rc.6` será recriada após integrar a correção.

## Handoff

- (preencher ao final: PR, tag, run, assets, checksums, smokes, decisão)
