# SB-021 — Promoção v1.0.0 estável

Estado: concluída (v1.0.0 publicada como estável em 13/09/2026).

- Objetivo: promover a suíte a `v1.0.0` estável conforme a decisão SB-019r
  (blocos A–F verdes sobre o rc.7, descarte comprovado), corrigindo os dois
  hashes documentais apontados na revisão e o smoke RPM no caminho estável.
- Base: `6ad5ba4` (`main` com SB-020 + SB-019r integradas).
- Dependências: SB-019r (recomendação), SB-020 (rc.7), SB-018 (roteiro).

## Escopo

- `VERSION=1.0.0` (conversões: app `1.0.0`, deb `1.0.0-1`, rpm `1.0.0`/`1`,
  prerelease `false`); correções documentais P1 (hashes rc.7) e P2
  (redação "regressão equivalente", dívida Novo/Abrir/Demo) — PR #51.
- Correção do `install_smoke.sh` no caminho estável: a asserção "NEVR
  precede `1.0.0-1`" só vale para prereleases; no estável (`release == 1`)
  o NEVR com sufixo de distro É o estável — PR #52. A escada
  alpha/beta/rc segue verificada.
- Tag `v1.0.0`, validação dos sete artefatos estáveis e publicação estável.

## Fora do escopo

- Mudanças de produto além de `VERSION` (nenhuma; binários derivam da
  mesma árvore validada no rc.7 + testes).
- Reutilizar hashes do rc.7 nos artefatos estáveis (recalculados abaixo).

## Aceite

- Workflow da tag verde nos seis jobs; smokes locais nos 4 formatos sobre
  os artefatos baixados; release pública, estável (não draft/prerelease).

## Handoff — v1.0.0 publicada (2026-09-13)

- Preparação: PR #51 (`chore(release): promove v1.0.0 e corrige hashes do
rc.7`), integrado em `e52626b`; gates verdes.
- Intercorrência: workflow da tag (run `34784292120`) falhou no
  `install-smoke` RPM — `1.0.0-1.fc46 deve preceder 1.0.0-1
(rpmdev-vercmp=11)`; `release` pulado, nenhum draft criado. Causa: o
  smoke fora escrito só para prereleases. Correção no PR #52 (merge
  `c4bb487`), provada localmente com os RPMs `1.0.0-1.fc46` do próprio run.
  Detalhe: `trunk-check` do #52 falhou uma vez por infra ("Text file busy"
  no hadolint, zero issues); verde no re-run.
- Tag `v1.0.0` movida para `c4bb487` (force-update; nada havia consumido a
  tag — sem release associada; precedente SB-018 no rc.6). Workflow run
  `34785587824`: `deb-ubuntu`, `deb-debian`, `rpm`, `appimage`,
  `install-smoke` e `release` com sucesso.
- Assets estáveis (SHA-256 recalculados sobre os arquivos baixados):
  - `055a643da06a4f8a694b4214b885dbe7b78209c3b0d21385883fd0b1b6b6c34d`
    `showbox_1.0.0-1_ubuntu24.04_amd64.deb`;
  - `be711037865bef78003d13d3109127d2ee262e064a2dc169679ced256bce7a26`
    `showbox_1.0.0-1_debian13_amd64.deb`;
  - `21cff08b76759648b524ea1fd334bf34c9f64c67cceaaec81d084d1153aca09f`
    `showbox-1.0.0-1.fc46.x86_64.rpm`;
  - `13eef286697fc7d4b1109de3ba9c9d4e5c17e845ef18d23203c0f945ee839da3`
    `showbox-studio_1.0.0-1_ubuntu24.04_amd64.deb`;
  - `847c32efad534ba4cac76f76ef64fae7c968af346778f63f80d368a2ab4d2ae0`
    `showbox-studio_1.0.0-1_debian13_amd64.deb`;
  - `3c8aaad3e3354b1d3ff700370b85e59209eb76b50e1d25dbbdaa3e6b596d0505`
    `showbox-studio-1.0.0-1.fc46.x86_64.rpm`;
  - `622cc3394109a26bb00e2431afee51047d4cd32300e00e4facbdb4cf34370b25`
    `ShowBox-Studio-1.0.0-x86_64.AppImage`.
- Smokes locais (podman, contêineres limpos, todos `1.0.0`): deb Ubuntu OK,
  deb Debian OK, RPM Fedora OK, AppImage OK.
- Release publicada como estável em 13/09/2026:
  `https://github.com/helton-godoy/showbox/releases/tag/v1.0.0`
  (`draft: false`, `prerelease: false`).
- Limitações: sem assinatura/notarização; GUI inferida por cadeia
  reprodutível. Backlog: órfão de `AddWidgetCommand` no produto, edição
  direta no canvas, testes ponta a ponta de Novo/Abrir/Demo.
